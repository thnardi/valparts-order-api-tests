{% extends "layout.twig" %}
{% set nav_active = 'posts' %}


{% block content -%}

  <section class="" >
    <div class="container">
      <div class="row">
        <div class="col-sm-12">
          <img src="{{ base_url() }}/{{ post.img_featured }}" style="width:500px;">
          <h3>{{ post.name }}</h3>
          <p>{{ post.description }}</p>
        </div>
      </div>
    </div>
  </section>

{%- endblock %}
{% block javascripts %} {{ parent()}}
{% endblock %}
