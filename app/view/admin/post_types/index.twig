{% extends "admin/layout.twig" %}
{% block html_title %}Categorias - Administração - {{ parent() }}{% endblock %}
{% set nav_active = 'posts' %}
{% set subnav_active = 'post_types' %}
{% block breadcrumbs %}
<ol class="breadcrumb">
  <li><a href="{{ base_url() }}/admin">Início</a></li>
  <li class="active">Categorias de Postagens</li>
</ol>
{% endblock %}
{% block content %}
<div class="row">
  <div class="col-xs-12 col-sm-4">
    <p class="">
      <a class="btn btn-success" href="{{ base_url() }}/admin/post_types/add">
        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Cadastrar
      </a>
    </p>
  </div>
</div>
<div class="panel panel-default">
  <div class="panel-heading" >
    <div class="row">
      <div class="col-xs-12">
        <p class="panel-title">Categorias</p>
      </div>
    </div>
  </div>
  <div class="panel-body">
    <div class="row">
      <div class="col-xs-12">
        {% if post_types %}
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Nome</th>
                  <th>Slug</th>
                  <th>Descrição</th>
                  <th class="th-button" style="width:150px; text-align:center;">Ações</th>
                </tr>
              </thead>
              <tbody>
                {% for post_type in post_types %}
                  <tr>
                    <td>
                      <a href="{{ base_url() }}/admin/post_types/{{ post_type.id }}">
                        {{ post_type.name }}
                      </a>
                    </td>
                    <td>{{ post_type.slug }}</td>
                    <td>{{ post_type.description }}</td>
                    <td style="width:150px;">
                      <center>
                        <a class="btn btn-sm btn-default" style="margin-top:2px; margin-bottom:2px;" href="{{ base_url() }}/admin/post_types/edit/{{ post_type.id }}">
                          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        </a>
                        <a class="btn btn-sm btn-danger" style="margin-top:2px; margin-bottom:2px;" href="{{ base_url() }}/admin/post_types/delete/{{ post_type.id }}">
                          <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                        </a>
                      </center>
                    </td>
                  </tr>
                {% endfor %}
              </tbody>
            </table>
          </div>
        {% else %}
          <div class="alert alert-danger" role="alert">Não existem posts cadastrados</div>
        {% endif %}
      </div>
    </div>
  </div>
</div>

{% endblock %}
