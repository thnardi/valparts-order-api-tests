{% extends "admin/layout.twig" %}
{% block html_title %}Usuários - Administração - {{ parent() }}{% endblock %}
{% set nav_active = 'user' %}
{% block breadcrumbs %}
<ol class="breadcrumb">
  <li><a href="{{ base_url() }}/admin">Início</a></li>
  <li class="active">Usuários</li>
</ol>
{% endblock %}
{% block content %}

<div class="card">
  <div class="card-header" data-background-color="blue">
    <div class="row">
      <div class="col-sm-8">
        <h3>{{ page_title }}</h3>
      </div>
      <div class="col-sm-4">
        <p class="pull-right">
          {% if users -%}
            <a class="btn btn-default" href="{{ base_url() }}/admin/user/export">
              <span class="glyphicon glyphicon-export" aria-hidden="true"></span>
            </a>
          {%- endif %}
          <a class="btn btn-success" href="{{ base_url() }}/admin/user/add">
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
          </a>
        </p>
      </div>

    </div>

  </div>
  <div class="card-content">
    <div class="row">
      <div class="col-xs-12">
        {% if users %}
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Email</th>
                  <th>Nome</th>
                  <th>Criado em</th>
                  <th>Cargo</th>
                  <th class="th-button"></th>
                  <th class="th-button"></th>
                </tr>
              </thead>
              <tbody>
                {% for user in users %}
                <tr>
                  <td>
                    <a href="{{ base_url() }}/admin/user/{{ user.id }}">
                      {{ user.email }}
                    </a>
                  </td>
                  <td>{{ user.name }}</td>
                  <td>{{ user.created_at }}</td>
                  <td>{{ user.role }}</td>
                  <td>
                    <a class="btn btn-xs btn-default" href="{{ base_url() }}/admin/user/edit/{{ user.id }}">
                      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
                    </a>
                  </td>
                  <td>
                    <a class="btn btn-xs btn-danger" href="{{ base_url() }}/admin/user/delete/{{ user.id }}">
                      <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Apagar
                    </a>
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>
          </div>
        {% else %}
          <div class="alert alert-danger" role="alert">Não existem usuários cadastrados</div>
        {% endif %}
      </div>
    </div>
  </div>
</div>



{% endblock %}
